package http;

import com.google.gson.JsonElement;

public class KVTaskClient {
    int port;
    public KVTaskClient(int port) {
        this.port = port;
    }

    public JsonElement load(String tasks) {//TODO
        JsonElement jsonElement = null;

        return jsonElement;
    }
}
